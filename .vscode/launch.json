{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main.exe",  // musi istnieć po buildzie
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",   // musi pasować do label w tasks.json
            "MIMode": "gdb",
            "externalConsole": true
        }
    ]
}
